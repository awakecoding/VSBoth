<UserControl x:Class="VSBothExtension.VSCodeToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <Grid>
        <WindowsFormsHost x:Name="FormsHost" />
        
        <!-- Transparent overlay on edges to prevent VS Code resize cursor interaction -->
        <Border Width="2" HorizontalAlignment="Right" Background="Transparent" IsHitTestVisible="True" />
        <Border Width="2" HorizontalAlignment="Left" Background="Transparent" IsHitTestVisible="True" />
        <Border Height="2" VerticalAlignment="Top" Background="Transparent" IsHitTestVisible="True" />
        <Border Height="2" VerticalAlignment="Bottom" Background="Transparent" IsHitTestVisible="True" />
    </Grid>
</UserControl>
